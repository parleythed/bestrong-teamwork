
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingress-nginx-tls
  # namespace: test-ns

spec:
  secretName: userapi-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - bestandstrong.pp.ua
  usages:
    - digital signature
    - key encipherment
    - server auth
